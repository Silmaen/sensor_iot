#pragma once

// Copy this file to credentials.h and fill in your values.
// credentials.h is gitignored.

#define WIFI_SSID     "your_ssid"
#define WIFI_PASSWORD "your_password"
// Optional: set to the AP's channel to connect to a HIDDEN SSID on ESP32 (0 = auto).
// #define WIFI_CHANNEL  5

#define MQTT_SERVER   "192.168.1.100"
// MQTT_PORT is defined in config.h (default 1883)

// Optional: MQTT authentication
// #define MQTT_USER     "user"
// #define MQTT_PASSWORD "password"
